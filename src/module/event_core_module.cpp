#include "event_core_module.h"

bool EventCoreModule::event_handler(const std::vector<std::string>& v) {
    return true;
}

bool EventCoreModule::timer_resolution_handler(
        const std::vector<std::string>& v) {
    return true;
}
